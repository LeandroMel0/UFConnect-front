<!DOCTYPE html>
<html lang="pt-br">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>UFConnect</title>
  <link rel="icon" href="/icons/UFConnect.png" type="image/x-icon"> <!--Icone do site-->
  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

  <style>
    body {
      background-color: #0c0c1e;
      color: #fff;
    }

    header {
      background-color: #002f5f;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.4);
    }

    .logo {
      font-size: 24px;
      color: #d000ff;
    }

    .barra-pesquisa {
      border-radius: 50px;
      padding: 0.5rem 1rem;
      border: none;
      width: 100%;
    }

    .sidebar {
      background-color: #121223;
      border-radius: 10px;
      padding: 1rem;
      color: #fff;
    }

    .btn-custom {
      background-color: #a000f0;
      color: white;
      border-radius: 20px;
    }

    .btn-custom:hover {
      background-color: #8e2bb3;
    }

    .btn-red {
      background-color: #ff3b3b;
      /* Vermelho vibrante */
      color: white;
      border-radius: 20px;
    }

    .btn-red:hover {
      background-color: #b11414;
      /* Um tom mais escuro no hover */
    }

    .postagem {
      background-color: #292942;
      border-radius: 10px;
      padding: 1rem;
      margin-top: 1rem;
    }

    .campo-texto {
      border: none;
      border-radius: 10px;
      resize: none;
      width: 100%;
      padding: 0.75rem;
    }

    .imagem-postagem {
      border-radius: 8px;
      margin-top: 10px;
      width: 100%;
    }

    .accordion-button {
      background-color: #1c1c2e;
      color: #fff;
      border-radius: 10px !important;
      box-shadow: none;
      transition: background-color 0.3s ease;
    }

    .accordion-button:not(.collapsed) {
      background-color: #2a2a40;
      color: #fff;
      border-bottom-left-radius: 0 !important;
      border-bottom-right-radius: 0 !important;
    }

    .accordion-body {
      border-top: 1px solid #444;
      border-bottom-left-radius: 10px;
      border-bottom-right-radius: 10px;
    }

    .accordion-button:focus {
      box-shadow: none;
    }

    .accordion-button::after {
      filter: brightness(0) invert(1);
      /* transforma a seta em branco */
    }

    .perfil-card {
      width: 100%;
      position: relative;
    }

    .posicao-foto-perfil {
      position: absolute;
      top: 35px;
      left: 50%;
      transform: translateX(-50%);
    }

    .foto-perfil {
      width: 60px;
      height: 60px;
      object-fit: cover;
      background-color: #fff;
    }

    .sidebar-links a {
      display: block;
      color: #ffffff;
      text-decoration: none;
      padding: 0.5rem 0.75rem;
      border-radius: 8px;
      transition: background-color 0.3s ease, color 0.3s ease;
    }

    .sidebar-links a:hover {
      background-color: #a000f0;
      color: #ffffff;
    }

    .btn-invisible {
      background: none;
      border: none;
      color: inherit;
      padding: 0;
      font: inherit;
      display: flex;
      align-items: center;
      gap: 6px;
      transition: all 0.2s ease-in-out;
    }

    .btn-invisible:hover {
      background-color: rgba(255, 255, 255, 0.05);
      border: 1px solid #b938c2;
      border-radius: 5px;
      cursor: pointer;
      padding: 4px 8px;
    }

    .btn-publicar-oportunidades {
      background-color: #ff7f00;
      /* laranja vibrante */
      color: white;
      border-radius: 20px;
    }

    .btn-publicar-oportunidades:hover {
      background-color: #cc6600;
      /* laranja escuro no hover */
    }

    .btn-white-custom {
      background-color: #ffffff;
      color: #121223;
      border: 1px solid #121223;
      border-radius: 20px;
      font-weight: 500;
      transition: all 0.2s ease-in-out;
    }

    .btn-white-custom:hover {
      background-color: #f0f0f0;
      color: #000;
      border-color: #000;
    }

    /*Para padronizara as imagens*/
    .object-fit-cover {
      object-fit: cover;
    }

    .link-feed-nome {
      color: white;
      text-decoration: none;
      font-weight: normal;
      transition: color 0.3s;
    }

    .link-feed-nome:hover {
      color: #b938c2;
      /* azul Bootstrap, ou qualquer cor */
    }
  </style>

</head>

<body>
  <!-- HEADER -->
  <header class="fixed-top w-100 py-3 px-4 d-flex justify-content-between align-items-center">
    <img src="pictures/UFConnect.png" alt="logo" width="150" height="50" class="logo-img align-self-center">
    <div class="input-group" style="width: 700px; margin-left: 1%;">
      <!-- Texto da publicação -->
      <input type="text" class="form-control form-control-sm barra-pesquisa" placeholder="Pesquisar...">
      <button class="btn btn-custom" type="button">
        <img src="icons/buscar.png" alt="Buscar" width="20" height="20">
      </button>
    </div>
    <nav class="d-flex gap-3 align-items-center">
      <!-- Botão: Início -->
      <button onclick="location.href='/feed'"
        class="btn text-white text-decoration-none d-flex align-items-center gap-2">
        <img src="icons/inicio.png" alt="Início" width="20" height="20">
        Início
      </button>

      <!-- Botão: Oportunidades -->

      <button onclick="location.href='/feedOpportunities'"
        class="btn text-white text-decoration-none d-flex align-items-center gap-2">
        <img src="icons/oportunidadesB.png" alt="Oportunidades" width="20" height="20">
        Oportunidades
      </button>


      <!-- Botão: Notificações -->
      <button onclick="" class="btn text-white text-decoration-none d-flex align-items-center gap-2">
        <img src="icons/notificacoesB.png" alt="Notificações" width="20" height="20">
        Notificações
      </button>

      <!-- Botão: Perfil -->
      <button onclick="location.href='/perfil/<%= user_info.id %>'"
        class="btn text-white text-decoration-none d-flex align-items-center gap-2">
        <img src="icons/perfil.png" alt="Perfil" width="20" height="20">
        Perfil
      </button>

    </nav>
  </header>

  <!-- CONTAINER -->
  <div style="padding-top: 120px;">
    <div class="row justify-content-center">

      <!-- Sidebar esquerda -->
      <aside class="col-lg-3 sidebar me-3" style=" width: 250px; margin-left: 0;">
        <div class="perfil-card text-center bg-light text-dark rounded overflow-hidden shadow-sm">
          <div class="banner-perfil"
            style="background-image: url('pictures/banner.jpg'); height: 80px; background-size: cover; background-position: center;">
          </div>

          <a href="/perfil/<%= user_info.id %>" class="posicao-foto-perfil p-0 border-0 bg-transparent">


            <img src="<%=user_info.avatar_url || " pictures/perfil.jpg"%>" alt="Foto de perfil"
            class="foto-perfil rounded-circle border border-3 border-white">

          </a>

          <div class="pt-4 pb-3">
            <h5 class="mb-0 fw-bold">
              <%=user_info.nome%>
            </h5>
            <p class="mb-1 text-muted small">
              <%=user_info.cargo%>
            </p>
            <p class="text-muted small mb-0">📍 <%=user_info.cidade%>, <%=user_info.estado%>
            </p>
          </div>
        </div>

        <ul class="list-unstyled sidebar-links">
          <li class="mb-2"></li>
          <li>
            <a href="#" class="text-white text-decoration-none d-flex align-items-center">
              <img src="icons/notificacoes.png" alt="Notificações" width="20" class="me-2">
              Notificações
            </a>
          </li>
          <li>
            <a href="#" class="text-white text-decoration-none d-flex align-items-center">
              <img src="icons/salvos.png" alt="Itens Salvos" width="20" class="me-2">
              Itens Salvos
            </a>
          </li>
          <li>
            <a href="#" class="text-white text-decoration-none d-flex align-items-center">
              <img src="icons/oportunidades.png" alt="Oportunidades" width="20" class="me-2">
              Oportunidades
            </a>
          </li>
          <li>
            <a href="#" class="text-white text-decoration-none d-flex align-items-center">
              <img src="icons/servicos.png" alt="Serviços UFC" width="20" class="me-2">
              Serviços UFC
            </a>
          </li>
        </ul>

        <div class="flex-grow-1 W-100">
          <div class="d-flex gap-2 mt-2">
            <!-- Botão para abrir modal de publicação -->
            <button class="btn btn-custom rounded-pill w-100 btn-lg fs-5 py-1" data-bs-toggle="modal"
              data-bs-target="#modalPublicar">
              Publicar
            </button>
          </div>
        </div>

        <div class="d-grid gap-2 mt-3">
          <div class="accordion" id="accordionExample">
            <div class="accordion-item bg-dark text-white border-0 rounded">
              <h2 class="accordion-header">
                <button class="accordion-button collapsed bg-dark text-white rounded d-flex align-items-center gap-2"
                  type="button" data-bs-toggle="collapse" data-bs-target="#flush-collapseOne" aria-expanded="false"
                  aria-controls="flush-collapseOne">

                  <img src="icons/configuracoes.png" alt="Configurações" width="20" height="20">
                  Configurações
                </button>

              </h2>
              <div id="flush-collapseOne" class="accordion-collapse collapse" data-bs-parent="#accordionExample">
                <div class="accordion-body bg-dark rounded-bottom">
                  <a href="/logout" class="text-white text-decoration-none d-block"> Sair</a>
                </div>
              </div>
            </div>
          </div>
      </aside>

      <!-- Conteúdo principal -->
      <main class="col-lg-6 d-flex flex-column">

        <!-- Caixa de postagens com rolagem -->
        <div class="flex-grow-1 overflow-auto bg-dark rounded p-3" style="max-height: 650px;">
          <% if (posts.length> 0) { %>
            <% posts.forEach(post=> { %>

              <!-- Postagem -->
              <div class="postagem bg-secondary bg-opacity-25 p-3 mb-3 rounded position-relative">

                <!-- Menu de três pontinhos -->
                <div class="dropdown position-absolute top-0 end-0 mt-4 me-3">
                  <button class="btn btn-sm btn-secondary bg-opacity-25 border-0 text-white-50" type="button"
                    data-bs-toggle="dropdown" aria-expanded="false">
                    &#x22EE;
                  </button>
                  <ul class="dropdown-menu dropdown-menu-end">
                    <li><a class="dropdown-item" href="#">Salvar publicação</a></li>
                  </ul>
                </div>

                <div class="mb-2">

                  <img src="<%=post.avatar_url || 'pictures/perfil.jpg'%>" alt="Foto de perfil"
                    class="rounded-circle me-3" width="50" height="50">
                  <strong><a href="/perfil/<%=post.usuario_id%>" class="link-feed-nome">
                      <%= post.nome %>
                    </a>
                  </strong>
                  <p class="mb-1">
                    <%= post.cargo %>
                  </p>
                </div>

                <p>
                  <%= post.texto %>
                </p>
                <%if(post.imagem_url){%>
                  <img src="<%=post.imagem_url%>" class="object-fit-cover w-100 h-100">
                  <%}%>
                    <div class="d-flex justify-content-around text-white-50" style="margin-top: 2%;">
                      <div class="d-flex gap-5">
                        <button class="btn-invisible like-button" type="button">
                          <img src="icons/curtida.png" alt="Curtir" width="20" height="20">
                          <span class="like-count">50</span>
                        </button>
                        <button class="btn-invisible">
                          <img src="icons/comentario.png" alt="Comentários" width="20" height="20"> 11 comentários
                        </button>
                        <button class="btn-invisible">
                          <img src="icons/compartilhar.png" alt="Compartilhar" width="20" height="20"> Compartilhar
                        </button>
                      </div>
                    </div>

              </div>

              <% }) %>
                <% } %>
                  <div class="spinner-border invisible" role="status"
                    style="position:sticky; top: 50%;left: 50%;width: 3rem; height: 3rem; z-index: 1050;">
                    <span class="visually-hidden">Loading...</span>
                  </div>

        </div>
      </main>

      <!-- Sidebar direita -->
      <aside class="col-lg-3 sidebar ms-3">
        <h5 class="d-flex align-items-center gap-2">
          <img src="icons/minhaRedeW.png" alt="Minha rede" width="25.37" height="18.44">
          Minha rede
        </h5>
        <ul class="list-unstyled">
          <li class="mb-2">
            <a href="#" class="text-white text-decoration-none d-flex align-items-center gap-2">
              <img src="icons/perfil.png" alt="Perfil" width="20" height="20">
              Professor Evilásio
            </a>
          </li>
          <li class="mb-2">
            <a href="#" class="text-white text-decoration-none d-flex align-items-center gap-2">
              <img src="icons/perfil.png" alt="Perfil" width="20" height="20">
              Prof. Elmano
            </a>
          </li>
          <li class="mb-2">
            <a href="#" class="text-white text-decoration-none d-flex align-items-center gap-2">
              <img src="icons/perfil.png" alt="Perfil" width="20" height="20">
              Prof. Jarbas
            </a>
          </li>
          <li class="mb-2">
            <a href="#" class="text-white text-decoration-none d-flex align-items-center gap-2">
              <img src="icons/perfil.png" alt="Perfil" width="20" height="20">
              Professora Nilena
            </a>
          </li>
        </ul>

      </aside>

    </div>
  </div>

  <!-- Modal de Publicação -->
  <div class="modal fade" id="modalPublicar" tabindex="-1" aria-labelledby="modalPublicarLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered modal-lg">
      <div class="modal-content bg-dark text-white rounded-4 position-relative"
        style="min-height: 350px; padding: 1.5rem;">

        <!-- Botão X fixado no topo -->
        <button type="button" class="btn-close btn-close-white position-absolute" style="top: 15px; right: 15px;"
          data-bs-dismiss="modal" aria-label="Fechar"></button>

        <!-- Corpo do modal -->
        <div class="modal-body pt-0">
          <!-- Foto + Caixa de texto -->
          <form id="postFrom">
            <div class="d-flex align-items-start mb-4 mt-2">
              <img src="<%=user_info.avatar_url || 'pictures/perfil.jpg'%>" alt="Foto de perfil"
                class="rounded-circle me-3" width="55" height="55">
              <textarea class="form-control" name="texto" placeholder="Compartilhe algo com seus colegas da UFC!"
                rows="6" style="resize: none; border-radius: 10px;"></textarea>
            </div>

            <!-- Linha branca separadora -->
            <hr style="border-top: 1px solid white; opacity: 1;" class="my-4">

            <!-- Imagem -->
            <div class="mb-4">
              <label for="imagem" class="form-label">Imagem</label>
              <input type="file" class="form-control" name="imagem" id="imagem" accept="image/*">

              <!-- Área de visualização -->
              <div class="mt-3">
                <img id="previewImagem" src="#" alt="Pré-visualização da imagem" class="img-fluid rounded"
                  style="display: none; max-height: 200px;">
              </div>
            </div>


            <!-- Acordeão de opções de publicação -->
            <div class="mb-4 d-none">
              <label for="destinoPublicacao" class="form-label">Onde publicar?</label>
              <select class="form-select" name="tipo" id="destinoPublicacao" required>
                <option value="0">Feed</option>
              </select>
            </div>

            <!-- Botões -->
            <div class="d-flex gap-4" style="margin-top: 2%;">
              <button id="btnPublicar" class="btn btn-custom w-100">Publicar</button>
            </div>
          </form>
        </div>
      </div>
    </div>
  </div>

  <!-- Bootstrap Bundle -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

  <script>
    document.querySelectorAll('.like-button').forEach(button => {
      button.addEventListener('click', () => {
        const post = button.closest('.postagem');
        const counter = post.querySelector('.like-count');
        let currentLikes = parseInt(counter.textContent);
        counter.textContent = currentLikes + 1;
      });
    });
  </script>

  <script>

    // Espera o DOM carregar
    document.addEventListener('DOMContentLoaded', () => {
      const btnPublicar = document.getElementById('btnPublicar');
      const radios = document.querySelectorAll('input[name="destinoPublicacao"]');

      radios.forEach(radio => {
        radio.addEventListener('change', () => {
          if (radio.value === 'oportunidades' && radio.checked) {
            // Muda para laranja
            btnPublicar.classList.add('btn-publicar-oportunidades');
            btnPublicar.classList.remove('btn-custom');
          } else if (radio.value === 'feed' && radio.checked) {
            // Volta para roxo original
            btnPublicar.classList.add('btn-custom');
            btnPublicar.classList.remove('btn-publicar-oportunidades');
          }
        });
      });
    });
  </script>


</body>

</html>

<!--Salva o token-->
<script>
  async function gettoken() {
    fetch('/get/token', {
      method: 'GET',
    })
      .then(res => res.json())
      .then(data => {
        if (data.token) {
          localStorage.setItem('authToken', data.token);
        }
      });
  }

  gettoken();

</script>

<!-- Visualiza Imagem-->
<script>
  document.getElementById('imagem').addEventListener('change', function (event) {
    const preview = document.getElementById('previewImagem');
    const file = event.target.files[0];

    if (file) {
      const reader = new FileReader();
      reader.onload = function (e) {
        preview.src = e.target.result;
        preview.style.display = 'block';
      }
      reader.readAsDataURL(file);
    } else {
      preview.src = '#';
      preview.style.display = 'none';
    }
  });
</script>

<!-- Scritp de envio de post-->
<script>
  document.getElementById("postFrom").addEventListener("submit", async function (e) {
    e.preventDefault();

    const form = e.target;
    const formData = new FormData(form);
    const token = localStorage.getItem("authToken");

    try {
      const response = await fetch("<%=API%>/post/create", {
        method: "POST",
        body: formData,
        headers: {
          Authorization: `Bearer ${token}`
        }
      });
      window.location.href = "/feed";
    } catch (error) {
      console.error(error);
      alert("Erro ao atualizar perfil.");
    }
  });
</script>
